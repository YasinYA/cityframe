# Mapbox
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# Replicate (for AI upscaling)
REPLICATE_API_TOKEN=your_replicate_token_here

# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/cityframe

# Redis
REDIS_URL=redis://localhost:6379

# Paddle (one-time payment)
PADDLE_API_KEY=your_paddle_api_key
PADDLE_WEBHOOK_SECRET=pdl_ntfset_...
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=test_...
PADDLE_PRO_PRICE_ID=pri_...
# PADDLE_ENVIRONMENT=sandbox  # Optional: use "sandbox" for testing, defaults to "production" in production
# NEXT_PUBLIC_PADDLE_ENVIRONMENT=sandbox  # Optional: client-side, same as above

# Resend (for email OTP)
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=City Frame <info@cityframe.app>

# Analytics (Mixpanel)
NEXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
